# VBSP-SCM — Cline Rules
# Cập nhật: 24/05/2026
# Đồng bộ với CLAUDE.md — đọc CLAUDE.md để biết toàn bộ quy tắc chi tiết.

---

# 1. DỰ ÁN & STACK

Hệ thống Quản trị Tín dụng Nội bộ — NHCSXH Chi nhánh Đồng Nai (đã sáp nhập Bình Phước 2025).

- **Stack:** Streamlit + Python + SQLite + PyArrow/Parquet
- **Chạy:** `streamlit run app.py` → http://localhost:8501
- **~20 users, 9 vai trò, 22 đơn vị (Hội sở + 21 PGD), 95 xã**

---

# 2. TRƯỚC KHI CODE — BẮT BUỘC

1. **Đọc BUGMAP.md** — section liên quan đến file/thao tác sắp sửa để tránh lặp lỗi cũ
2. Chỉ đọc file thực sự liên quan đến task — không đọc toàn bộ codebase
3. Đọc file cần sửa trước khi edit (view phần liên quan, không cần toàn bộ)
4. Grep ngược để xem hàm nào đang gọi hàm sắp sửa

---

# 3. CẤU TRÚC FILE — BẢN ĐỒ NHANH

| Sửa gì | Đọc trước | Sửa ở đây |
|---|---|---|
| Logic tab UI | `tabs/tab_*.py` liên quan | Tab đó |
| Merge 22 PGD | `upload_service.py` ~dòng 288-480 | `merge_du_lieu_toan_cn()` |
| Phân quyền | `auth.py`, `ROLES.md` | `auth.py` |
| Hằng số cột/chương trình | `config.py` | `config.py` |
| Xuất Word/PDF | `services/template_service.py` | Template + service |
| Workspace CN | `workspaces/ws_management.py` | File đó |
| Workspace PGD | `workspaces/ws_operation.py` | File đó |
| Upload file | `services/upload_service.py` | `luu_pgd_file()` / `luu_file_he_thong()` |
| Dữ liệu kv_store | `db.py` | `doc_kv()` / `ghi_kv()` |
| Snapshot HSTD | `snapshot_service.py` | upsert-safe |
| Giao ban | `data/giao_ban.py` | `tinh_so_lieu_van_xuoi()` |

---

# 4. LƯU TRỮ — CHỈ DÙNG kv_store

```python
# ĐỌC
value = db.doc_kv("key_name")              # None nếu không có
values = db.doc_kv_prefix("khtd_pgd_")    # nhiều key cùng prefix

# GHI — luôn kèm username
db.ghi_kv("key_name", value, username)
```

Key chuẩn:

| Key | Dùng cho |
|---|---|
| `khtd_cn` | KHTD Chi nhánh |
| `khtd_pgd_{slug}` | KHTD PGD |
| `ct_registry_{slug}` | Chương trình theo PGD |
| `merge_meta_{loai}` | Metadata merge (hstd/nq11/gqvl) |
| `kehoach` | KH Điện báo toàn CN |
| `kehoach_pgd_{slug}` | KH Điện báo PGD |
| `no_rui_ro_{slug}_{yyyy}_{mm}` | Hồ sơ rủi ro |
| `kh_gqvl_cn_{nam}` | KH GQVL Chi nhánh |
| `kh_gqvl_pgd_{slug}_{nam}` | KH GQVL theo PGD (dự phòng) |
| `dgd_map` | Điểm giao dịch toàn CN |
| `khnv_phan_cong_list` | Phân công cán bộ nội bộ Phòng KH-NV |
| `khnv_lich_list` | Lịch công tác Phòng KH-NV |

`slug` = `pgd_slug(ten_pgd)` từ `data/pgd.py`

**KHÔNG dùng:** `json.dump()`, `open(file,'w')`, `session_state` để persist

---

# 5. AUDIT LOG — SAU MỌI THAO TÁC GHI

```python
username = st.session_state.get("username", "unknown")
db.ghi_audit(username, "ten_action", "mô tả cụ thể")
```

Action chuẩn: `luu_khtd_cn`, `luu_khtd_pgd`, `upload_hstd`, `merge_hstd`,
`luu_no_rui_ro`, `xuat_bieu_cn`, `xuat_01xln`, `upload_dienbao`

---

# 6. UPLOAD FILE — LUÔN QUA upload_service.py

```python
from services.upload_service import luu_pgd_file, luu_file_he_thong, KetQuaUpload

# ⚠️ luu_pgd_file chỉ có 3 tham số — KHÔNG có username
ket_qua = luu_pgd_file(ten_pgd, loai, file_bytes)
ket_qua.hien_thi()

# Sau upload thành công — BẮT BUỘC
st.cache_data.clear()
# Ghi audit thủ công sau luu_pgd_file
db.ghi_audit(username, "upload_hstd", f"Upload HSTD: {ten_pgd}")
```

**KHÔNG** ghi file trực tiếp từ tab, hardcode đường dẫn.

---

# 7. TIỀN TỆ — QUY ƯỚC 3 LỚP

| Lớp | Đơn vị | Ghi chú |
|---|---|---|
| Nhập liệu | Triệu đồng | `number_input` |
| Lưu trữ | VND (× 1_000_000) | kv_store / DataFrame |
| Hiển thị bảng | `fmt_ty()` `/1e6` | Header cột ghi "(triệu đồng)" |
| Hiển thị metric | `vn(x/1e9, 3) + " tỷ"` | Inline card |

```python
from utils import fmt, fmt_ty, fmt_tien, fmt_pct, fmt_so
fmt_ty(gia_tri_vnd)      # → "1.235"  (cột bảng, triệu đồng)
fmt_so(so_luong)         # → "1.234"  (số nguyên VN)
```

**KHÔNG** dùng `NumberColumn(format="%.3f")` — kiểu Mỹ, sai format VN.
Chuyển float → string bằng `.apply(fmt_ty)` trước `st.dataframe`.

---

# 8. PHÂN QUYỀN — DÙNG HELPER TỪ auth.py

```python
from auth import la_phan_he_cn, la_phan_he_pgd, normalize_role
from auth import co_quyen_upload_pgd, co_quyen_quan_ly_user_pgd

role = normalize_role(role)   # LUÔN normalize trước khi check

if la_phan_he_cn(role):       # executive, admin_cn, admin, manager_cn, manager, chuyenvien_cn
if la_phan_he_pgd(role):      # admin_pgd, manager_pgd, user_pgd, user
```

Bảng 9 role đầy đủ:

| Role | Phân hệ | Quyền |
|---|---|---|
| `executive` | CN | Chỉ đọc dashboard |
| `admin_cn` / `admin` | CN | Toàn quyền |
| `manager_cn` / `manager` | CN | Upload, giao chỉ tiêu |
| `chuyenvien_cn` | CN | Tác nghiệp CN (không upload) |
| `admin_pgd` | PGD | Upload + quản lý user PGD |
| `manager_pgd` | PGD | Upload + nhập kế hoạch |
| `user_pgd` / `user` | PGD | Chỉ thấy PGD mình |

**KHÔNG** check role bằng chuỗi thô `if role == "admin"`.

---

# 9. TÊN ĐƠN VỊ — DÙNG CONSTANT

```python
from config import DON_VI_CHI_NHANH, TEN_CHI_NHANH_HIEN_THI

DON_VI_CHI_NHANH       = "Hội sở Chi nhánh tỉnh"             # lọc df
TEN_CHI_NHANH_HIEN_THI = "Chi nhánh NHCSXH tỉnh Đồng Nai"   # hiển thị UI
```

---

# 10. WIDGET KEY — TRÁNH DuplicateElementKey

```python
# Khi hàm render được gọi nhiều lần (CN + PGD mode)
key_prefix = "cn_"    # hoặc f"pgd_{pgd_slug(pgd_user)}_"
st.selectbox("PGD", options, key=f"{key_prefix}ten_widget")
st.button("Lưu",            key=f"{key_prefix}btn_luu")
```

---

# 11. pgd_mode — PATTERN SONG SONG 2 PHÂN HỆ

```python
pgd_mode = kwargs.get("pgd_mode", False)
pgd_user = kwargs.get("pgd_user")

if pgd_mode:
    path = duong_dan_pgd(pgd_user, "dienbao_ht")
    key  = f"kehoach_pgd_{pgd_slug(pgd_user)}"
else:
    path = DB_HT_CACHE
    key  = "kehoach"
```

---

# 12. PATTERN TAB MỚI — TEMPLATE CHUẨN

```python
def render(tab=None, **kwargs) -> None:
    # 1. Giải kwargs
    df       = kwargs.get("df")
    df_full  = kwargs.get("df_full", df)
    role_raw = str(kwargs.get("role", "user") or "user")
    role     = normalize_role(role_raw)
    username = kwargs.get("username", "unknown")
    pgd_user = kwargs.get("pgd_user")

    # 2. Tab context — KHÔNG viết `with tab:` trực tiếp
    ctx = tab if tab is not None else st.container()
    with ctx:
        # 3. Guard
        if df is None or df.empty:
            st.warning("⚠️ Chưa có dữ liệu.")
            return
        # 4. Logic...
```

---

# 13. CSS & UI

- Inject CSS **một lần** trong `app.py` — không inject trong tab
- Bảng ≥ 8 cột → HTML thuần + `st.markdown(unsafe_allow_html=True)`
- HTML dark mode: **KHÔNG** dùng `color:black` hay `background:white` hardcode — dùng CSS variable hoặc inline style tương thích dark mode
- `st.date_input` bắt buộc có `format="DD/MM/YYYY"`

---

# 14. DUCKDB — KIỂM TRA SCHEMA TRƯỚC KHI QUERY

```python
import duckdb, pyarrow.parquet as pq

# PHẢI kiểm tra schema trước
schema = pq.read_schema(CACHE_HSTD)
cols = [f.name for f in schema]
if "Ten_ct" not in cols:
    st.warning("Parquet thiếu cột Ten_ct")
    return

# Sau đó mới query
df = duckdb.query(f"SELECT ... FROM '{CACHE_HSTD}'").df()
```

---

# 15. ERROR LOGGING — KHÔNG NUỐT LỖI

```python
from logger import get_logger
logger = get_logger(__name__)

try:
    xu_ly_gi_do()
except Exception as e:
    logger.error("ten_ham: mo_ta — %s", e, exc_info=True)
    db.ghi_audit(username, "loi_he_thong", f"{e}")
    st.error(f"❌ Lỗi: {e}")
# KHÔNG: except Exception: pass
# KHÔNG: except Exception as e: print(e)
```

---

# 16. LỖI THƯỜNG GẶP — TÓM TẮT BUGMAP

| Lỗi | File | Fix nhanh |
|---|---|---|
| `ArrowInvalid: Could not convert` | `upload_service.py` merge | Ép dtype trước concat |
| `DataType(null)` khi merge GQVL | `data/core.py` | Bỏ `dtype_backend='pyarrow'` |
| `DuplicateElementKey` | Mọi tab | Thêm `key_prefix` vào widget key |
| `fillna("") on int64` | `upload_service.py` | `ser.astype(object)` trước |
| Bảng trả về `None` | Tab groupby | Wrap try-except, log exception |
| DuckDB `Referenced column not found` | Tab dùng parquet | Check schema trước query |
| `luu_pgd_file username` | Upload tab | Hàm chỉ có 3 params, ghi audit riêng |

Đọc `BUGMAP.md` để xem chi tiết và các lỗi khác.

---

# 17. XUẤT WORD/PDF — DÙNG template_service.py

```python
from services.template_service import dien_template, nut_tai_word_va_pdf
from services.template_service import TMPL_THONG_BAO, TMPL_BAO_CAO

# Điền template và tạo nút tải
docx_bytes = dien_template(TMPL_THONG_BAO, {
    "don_vi": ten_don_vi,
    "ngay":   fmt_ngay(date.today()),
})
nut_tai_word_va_pdf(docx_bytes, "ten_file.docx", key="btn_tai_bc")
```

---

# 18. GIT — TUYỆT ĐỐI

- **KHÔNG** tự `git commit`, `git push`, `git add` — người dùng tự commit qua GitHub Desktop
- **LUÔN** sửa file tại `D:/VBSP-SCM` (worktree gốc, branch `main`)
- Sau khi sửa: `git status` tại `D:/VBSP-SCM` để xác nhận file đã modified

---

# 19. KHÔNG THÊM DEPENDENCY MỚI

Thư viện đã có:
`pandas`, `openpyxl`, `pyarrow`, `python-docx`, `docx2pdf`,
`streamlit`, `duckdb`, `concurrent.futures`, `threading` (stdlib),
`bcrypt`, `plotly`, `reportlab`, `gspread`

Trước khi dùng thư viện mới → kiểm tra đã có chưa.

---

# 20. SAU KHI APPLY — BẮT BUỘC

### CHANGELOG.md — thêm lên ĐẦU FILE

```
## [YYYY-MM-DD] — <mô tả ngắn>
- `filename.py` dòng ~X — mô tả thay đổi
```

- Dùng ngày thực tế hôm nay
- Mỗi file thay đổi = 1 dòng
- KHÔNG xóa entry cũ
- Áp dụng cho MỌI thay đổi kể cả fix nhỏ

### BUGMAP.md — thêm entry nếu vừa fix bug

Phân loại đúng section (A. Parquet, B. Streamlit UI, C. DataFrame, D. Database, E. Upload, F. PDF/Word, G. KHTD, H. GSheet, I. Role, J. Code Pattern).

---

# 21. TÀI LIỆU THAM CHIẾU

| File | Đọc khi |
|---|---|
| `DELTA.md` | **ĐỌC ĐẦU MỖI PHIÊN** — thay đổi gần đây, component mới, signature đã cập nhật |
| `CLAUDE.md` | Convention đầy đủ, luồng dữ liệu, pattern chuẩn |
| `BUGMAP.md` | Gặp lỗi — tra trước khi debug |
| `ARCHITECTURE.md` | Quan hệ import giữa các module |
| `TROUBLESHOOTING.md` | Lỗi vận hành thường gặp |
| `CHANGELOG.md` | Lịch sử thay đổi |
| `SCHEMA.md` | **ĐỌC TRƯỚC KHI VIẾT SQL** — schema 16 bảng SQLite + parquet + query mẫu |

---

# 22. CHECKLIST CUỐI TRƯỚC KHI APPLY

```
□ Đọc BUGMAP.md section liên quan — tránh lặp lỗi cũ
□ db.ghi_audit() sau mọi thao tác ghi
□ st.cache_data.clear() sau upload/ghi file
□ Upload qua upload_service.py (luu_pgd_file = 3 params, không có username)
□ Tiền: lưu VND, hiển thị fmt_ty() /1e6 (triệu đồng, 0 số lẻ)
□ normalize_role(role) trước khi check role
□ Phân quyền qua la_phan_he_cn() / la_phan_he_pgd() — không check string thô
□ Widget key unique (prefix khi pgd_mode=True)
□ render(tab=None, **kwargs) với fallback st.container()
□ len(tab_names) == len(_tab_renderers) nếu sửa ws_*.py
□ date_input có format="DD/MM/YYYY"
□ DuckDB: check schema parquet trước khi query
□ KHÔNG inject CSS trong tab — chỉ trong app.py
□ KHÔNG hardcode đường dẫn file
□ Cập nhật CHANGELOG.md
□ Cập nhật BUGMAP.md nếu fix bug
```
